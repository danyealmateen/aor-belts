import express from "express";
import { Barn } from "../schema/schema";

const router = express.Router();

router.delete("/deletekid", async (req,res) => {
    try {
        const deleteStudent = await 
    }
})
